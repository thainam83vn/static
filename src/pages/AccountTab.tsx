import React from "react";
import PageTemplate from "./PageTemplate";

const AccountTab: React.FC = () => {
  return (
    <PageTemplate title="Account" content={<>Account page</>}></PageTemplate>
  );
};

export default AccountTab;
